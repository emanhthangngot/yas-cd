apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yas-storefront
spec:
  ingressClassName: traefik
  rules:
    - host: yas.staging.local
      http:
        paths:
          - path: /swagger-ui
            pathType: Prefix
            backend:
              service:
                name: swagger-ui
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: storefront-ui
                port:
                  number: 3000
